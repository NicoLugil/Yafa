this file is wip...


IDE 1.5.6.r2
SD card with >=1 partition
OpenWrt installed on sda1: OpenWrt-Yun1.0

mnt/sda1: mkdir arduino/Yafa

opkg update 
opkg install:
   openssh-sftp-server
   python-expat
   setuptools
   python-openssl


edit etc/rc.local to:

touch /tmp/begin
wifi-live-or-reset
boot-complete-notify
sleep 5s
cd /mnt/sda1/arduino/Yafa
./main.py 1> /tmp/Yafa.log 2>&1 &
touch /tmp/end
exit 0

cp Yafa/python/* to /mnt/sda1/arduino/Yafa

mkdir private, in this dir make:

pw.py: containing usernames and passwors
__init__.py: empty file

put sketch on mcu

install feedparser


